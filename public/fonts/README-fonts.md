# Shunya Chakra Fonts

Place the commercial font files in this directory with these exact names:
- `Burowai.otf` or `Burowai.ttf` (Currently provided)
- `Galacthic-rgRPB.otf` (Currently provided)

They will be loaded via `next/font/local` in `app/fonts.ts`.
